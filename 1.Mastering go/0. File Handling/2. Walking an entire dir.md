To walk an entire Dir and apply a function to each file

```go
package main

import (
	"os"
	"path/filepath"
)

func callback(path string, dir os.DirEntry, dirErr error) (err error){
	info , _ := dir.Info()
	


}
```